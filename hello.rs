// Rust:
//
// is a modern C or C++ and do what they do but much better
// a low level language that garantee memory-safety
// has a rich type system and ownership model guarantee memory-safety and thread-safety
// is blazingly fast and memory-efficient: with no runtime or garbage collector
// the code will not compile if using memory in a wrong way
// dont have null value or exceptions! (use the type system to guarantee all)
//
// Environment:
//
// rustc <- compiler
// cargo <- pakage manager
// rustup <- versions manager

fn main() {
    println!("Hello World")
}
